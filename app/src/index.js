import React from "react";
import { createRoot } from 'react-dom/client';
function App(){ return(<div><h2>🚀 Jenkins CI/CD on EKS</h2></div>); }
createRoot(document.getElementById('root')).render(<App />);

